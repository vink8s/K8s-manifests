apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: role-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: react-app-clusterrole
subjects:
  - kind: USer
    name: Vinayaka # Change this to the desired user
    apiGroup: rbac.authorization.k8s.io
